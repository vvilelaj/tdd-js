QUnit.test( "hello first test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
});

QUnit.test( "hello second test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
});

QUnit.test( "hello third test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
});